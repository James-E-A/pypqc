
[project]
name = "pypqc"
version = "0.0.0.3"

[tool.setuptools.packages.find]
include = ["pqc"]
namespaces = false

[tool.setuptools.exclude-package-data]
"pqc" = ["*.c"]

[build-system]
requires = [
    'setuptools',
    'cffi',
]
build-backend = "_build_pep517"
backend-path = ["."]
